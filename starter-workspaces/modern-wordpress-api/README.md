# Modern WordPress API Starter Workspace

Add task-specific plugin code under `plugins/`.

The benchmark harness treats this starter workspace as the submitted project
surface for modern WordPress API tasks. Scenario manifests declare `plugins/` as
the writable root; hidden graders, prompts, scenarios, and workflow files are
not part of the submitted artifact contract.
